#include <stdio.h>

int power(int base, int exp) {
    if (exp == 0) return 1;
    return base * power(base, exp - 1);
}

int main() {
    int b = 2, e = 3;
    printf("%d^%d = %d", b, e, power(b, e)); 
    return 0;
}

OUTPUT :
2^3 = 8

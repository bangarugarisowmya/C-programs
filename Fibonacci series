#include <stdio.h>

int fib(int n) {
    if (n <= 1) return n;
    return fib(n-1) + fib(n-2);
}

int main() {
    int n = 5;
    for(int i = 0; i < n; i++)
        printf("%d ", fib(i)); // Output: 0 1 1 2 3
    return 0;
}

OUTPUT :
0 1 1 2 3 
